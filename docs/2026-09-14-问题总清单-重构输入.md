# shiyi-music 问题总清单（重构输入）

来源：2026-09-14 会话多轮核实。每条带证据位置（file:line 或验证方式）、严重度、状态。
严重度：P0=核心功能失效 / P1=结构性缺陷 / P2=体验缺陷 / P3=工程与流程。
状态：✅已修（v0.5.5，commit 7c7b5b5）/ ❌未修 / ⏳待确认 / 👤需用户操作。

---

## 一、API Key 存储链路（本轮已修复，v0.5.5）

| # | 问题 | 严重度 | 状态 | 证据 |
|---|---|---|---|---|
| 1 | Key 输入框留空保存即删钥匙串真 key（受控输入+空值 keychain_delete 构成数据丢失回路） | P0 | ✅已修 | 原 useSettings.ts:222-228；现"留空=沿用+读回恢复" |
| 2 | 每按键直连密钥同步，按键中间态写残缺 key 进钥匙串（中途退出留残值） | P0 | ✅已修 | 800ms 防抖（useSettings.ts scheduleSync） |
| 3 | cli_get 把任何非零退出当"条目不存在"，ACL 拒绝等真实错误静默吞没（导致上一会话误诊） | P1 | ✅已修 | classify_get_exit：仅 exit 44 视为不存在 |
| 4 | security CLI 依赖进程 PATH（防御性） | P2 | ✅已修 | /usr/bin/security 绝对路径 |
| 5 | 钥匙串操作无审计日志（追溯困难） | P2 | ✅已修 | cli_set/delete tracing |
| 6 | use_security_cli 死代码 | P3 | ✅已修 | 已删除 |
| 7 | 迁移标记无条件置位（无明文可迁时也置 MIGRATED=1，未来明文迁移路径依赖此标记） | P3 | ⏳待确认 | useSettings.ts:130-147；无实际损害证据，挂起观察 |
| 8 | 用户 key 与站点错配（讯飞 key 配商汤站→401，被误判"key 失效"） | P0 | ✅已解 | 应用配置已切回讯飞 MaaS（maas-api.cn-huabei-1.xf-yun.com/v2 + xopdeepseekv4flash0731），测试连接"连接成功" |

## 二、反馈与人工介入链路（全部未修）

| # | 问题 | 严重度 | 状态 | 证据 |
|---|---|---|---|---|
| 9 | 反馈功能只挂"最新一次生成"（status===done 且非 readOnly），历史记录一律只读（只有复制按钮）——用户对任何旧结果无法反馈修改 | P0 | ❌未修 | App.tsx:959-964（historyView→readOnly+onRefine空实现）；ResultPanel.tsx:180/198 |
| 10 | 生成出错/取消后无反馈入口（status 为 error/idle 时按钮不渲染） | P2 | ❌未修 | ResultPanel.tsx:180/209 status==="done" 条件 |
| 11 | 反馈按钮可发现性差：小按钮藏在大段输出最底部，用户"找了八遍没找到" | P2 | ❌未修 | ResultPanel.tsx:181-196 |
| 12 | 讨论轮自动推进，轮与轮之间无人工插话/暂停/确认门——用户无法在专家讨论中途输入意见 | P0 | ❌未修 | orchestrator.rs 轮循环无用户等待点；App.tsx 无插话通道 |
| 13 | 发言打架（界面层）：同轮专家并发执行+流式交错，多头像同时"思考中"、发言乱序 | P1 | ❌未修 | orchestrator.rs:1874-1904 join_all 并发（设计行为，R4 仅 2s 错峰）；usePipeline.ts:298 speechCb 交错追加 |
| 14 | 发言打架（内容层）：同轮专家互盲——看不到彼此本轮修订（只见上一轮"已提修订"），落到同一 target 的冲突修订无人裁决（主持人被限定"只拼接不改细节"） | P1 | ❌未修 | orchestrator.rs:1874 注释"同轮角色互相无依赖"；roles.rs 主持人 prompt"只做整合" |

## 三、知识注入架构（核心缺陷区，全部未修）

| # | 问题 | 严重度 | 状态 | 证据 |
|---|---|---|---|---|
| 15 | 思维资产 8 条上限+打分恒 0+稳定排序截断 → **每次只注入 CSV 文件顺序前 8 条**：lyric_craft 30 条可达仅 LC-01~08 注入（后 22 条永不生效），compose_craft 25 条可达仅前 8 注入（后 17 条永不生效）。用户补充内容追加在文件尾部=永远休眠。**"补充了太多内容根本没生效"的实锤机制** | P0 | ❌未修 | orchestrator.rs:441 `("trigger", &["审改"])`+Some(8)；knowledge.rs:347-427 打分=方案中"审改"出现次数×3（恒 0）；knowledge.rs:417 稳定排序 take(8)；实测注入清单 LC-01~08/CC-01~09 |
| 16 | 引用悬空：各角色 system_prompt 大量引用 LC-xx/CC-xx 编号（LC-13/15/16/18/23/26、CC-10/12/17/22/24 等）并要求"逐项核查"，但被引内容 90% 不在注入的 8 条里——**角色被要求核查一条看不见内容的规则** | P0 | ❌未修 | roles.rs 各角色 prompt（emotion 引 LC-15/18，lyricist 引 LC-01/02/04/15/16/18/26，producer 引 CC-01/12/17/22/24 等）vs #15 注入清单 |
| 17 | compose_craft trigger="全程" 的 2 条（CC-23、CC-28）被过滤器误伤，任何角色不可达 | P1 | ❌未修 | trigger 分布实测 + orchestrator.rs:441 只匹配"审改" |
| 18 | trigger="扩展位" 5 条（LC-29、CC-05/19/25）默认永不注入（预留设计，但用户补充落入扩展位即静默失效） | P2 | ❌未修（需用户可见的登记说明） | orchestrator.rs:439 注释 |
| 19 | suno_rules 40 条格式/数值规则：校验员全量、制作人 22 条子集、**情感/作词/改词/风格分析师 0 条** | P1 | ❌未修 | roles.rs 各角色 knowledge_tables（仅 auditor+producer 绑定 suno_rules） |
| 20 | 校验员死循环打回的结构根因：全量格式规范锁在流水线最后一环，上游（主持人）只有 400 字压缩清单，细节格式（说明行结构/能量标注格式/人声映射/乐器主次）上游看不到 → 上游排版错 → 校验员打回 → 恶性循环。用户判断"问题在排版不在校验员"成立 | P0 | ❌未修 | rules.rs:124-127 checklist（压缩清单）；roles.rs auditor prompt（全量细节） |
| 21 | 数值双源不同步：角色 prompt 内嵌硬编码数值文本（作词人"每行 6-13 字"）与 suno_rules.csv 数值脱钩——改 CSV 值，prompt 文本不联动（auditor 与 validator.rs 亦有同类双源警告） | P1 | ❌未修 | roles.rs lyricist prompt vs suno_rules.csv line_chars_*；roles.rs:65-66 注释自认 |
| 22 | 关键词表（emotions/cliches/hooks/style_genre/instruments）按方案文本命中注入——补充行的关键词若从不出现在方案里则永远休眠（静默失效，无任何告警） | P2 | ❌未修 | orchestrator.rs:399-427 matching_keywords 过滤 |
| 23 | trigger="阶段0" 行（LC-12）的实际注入路径未确认（注释称"仅主持 primer 用"，但主持人 knowledge_tables 为空，primer 组装是否读取 lyric_craft 待查） | P3 | ⏳待确认 | orchestrator.rs:439 注释 vs roles.rs host() 绑定 |

## 四、工程与发布

| # | 问题 | 严重度 | 状态 | 证据 |
|---|---|---|---|---|
| 24 | 本地 dmg 打包必挂（bundle_dmg.sh），依赖 CI 出 dmg | P3 | ❌既有基线 | 本地构建实测；CI 正常 |
| 25 | v0.5.5 停在草稿发布，公开最新版本仍是 v0.5.4（需用户登录 GitHub 点 Publish） | P2 | 👤需用户操作 | Releases 页面截图（草稿+3 平台资产已在） |
| 26 | Mimosa 完整安全扫描未出最终结论（有 Command.args 误报记录：实为参数向量 exec 无 shell） | P3 | ⏳待办 | 本会话工具记录 |
| 27 | 上一会话遗留设计差距：可搜索模型下拉（SearchableSelect）、厂商模板卡片、请求限流 UI 未做 | P3 | ❌未做 | 交接清单 |
| 28 | （外部）ZCode 星图 provider 标注 context 100 万，实际站点仅 20 万——长会话必错 | P3 | ❌未修（ZCode 配置层） | 模型列表 contextLen=200000 vs config |

## 修复包（待用户拍板顺序）

- **A｜注入层重构（治 15/16/17/19/20/21）**：思维资产相关性打分替代恒 0 打分（或分组轮换保证全量可见）；"引用必达"——prompt 引用的编号行强制注入；suno_rules 按角色职责扩绑（情感→参数类、作词→歌词格式类）；"全程" trigger 并入；数值占位符全量化消除硬编码双源；主持人整合 prompt 补全量格式规范（治死循环）
- **B｜并发与裁决（治 13/14）**：主持人整合前显式冲突检测（同 target 多修订先列冲突再裁决）；可选串行模式开关
- **C｜反馈与人工门（治 9/10/11/12）**：历史条目加"以此为基础修改"；轮间人工确认门（可关）；反馈按钮提升显著性
- **D｜工程收尾（24-28）**：发布草稿、重跑安全扫描、设计差距项

---

## 修复包 C：历史反馈 + 插话可发现性（2026-09-14 合并包，五步流程执行中）

### 诊断结论（修正此前误判）
- #12 插话：后端 interject.rs + 轮边界消费 + 前端 StatusIndicator 插话 UI **全链路已存在**，仅 running 窗口期可见（发现性为零）——呈现缺失，非功能缺失
- #9 历史反馈：App.tsx:962 `onRefine={() => {}}` + readOnly 硬编码断开——功能缺失
- 接线点数据完备：HistoryEntry 有 mode/input/output/conversation

### 方案（合并执行①②③）
- 阶段A（修复+新规则落地，同提交）：App.tsx 新增 handleRefineFromHistory(entry, feedback, refineMode)——装载 entry 会话进 chatHistoryRef/conversation/lastUserInput/lastFeedback → 复用 handleRefine 全链（不复制实现）；selectHistory/selectQueueItem 传 sourceEntry；历史 ResultPanel 接真 onRefine、去 readOnly、mode/locale 传真值；StatusIndicator running 提示补插话引导文案
- 边界：旧记录无 conversation → input/output 构造；token 冲突 → runTokenRef 既有机制；mode_c 原歌词 → lastUserInput 从 entry.input 重设
- 阶段B（旧规则清理，同提交）：删 onRefine={()=>{}} 空实现与历史 readOnly 硬编码
- 验证：红灯测试（历史 refine 收到正确 mode/lastOutput/feedback；旧格式兜底）→ vitest 全量 → tsc → GUI 双验收（历史条目优化出方案；运行中插话按钮可见可发）
- 回滚：单提交 revert
